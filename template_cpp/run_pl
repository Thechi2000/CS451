clear

for id in $(seq 1 2); do
    unbuffer bin/da_proc --id $id --hosts ../example/hosts --output ../example/output/$id ../example/configs/perfect-links.config | sed -u -e "s/^/[PROCESS $id] /" &
done

trap "trap - SIGTERM && kill -- -$$" SIGINT SIGTERM EXIT

wait
